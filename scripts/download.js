// Resume links point directly to the latest PDF in /resume via the
// download attribute in the markup, so no click interception is needed.
// (Kept as a module in case future download analytics are added.)
